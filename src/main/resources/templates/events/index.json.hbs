{
         "title": "{{event.name}}",
         "_self": "{{path_to_event}}",
         "id": {{event.id}},
         "date": "{{event.date}}"
         "name": "{event.name}}"
         "venue": {
                    "id": {{venue.id}},
                    "name": "{{venue.name}}"
                    "capacity": {{venue.capacity}},
                    "_self": "{{path_to_venue}}"
         }
}